import { Stack, Box } from "@mui/material";
import { VideoCard, ChannelCard, Loader } from "../";

const Videos = ({ videos }) => {
    // main {{videos}} componentdan chaqirildi
    // console.log(videos);//serverdan keletgan videolar kodni ildiziga qarab yahshilab qarab borish kerak

    if (!videos.length) return <Loader />;
    // // agar videosni uzunligi bo'lmasa yani serverdan malumotlar hali kelmagan bo'lsa loaderni ishlat yani qaytar

    return (
        <Stack
            width={"100%"}
            direction={"row"}
            flexWrap={"wrap"}
            justifyContent={"center"}
            alignItems={"start"}
            gap={3}
        >
            {videos.map((item, idx) => (
                <Box key={idx}>
                    {item.id.videoId && <VideoCard video={item} />}
                    {item.id.channelId && <ChannelCard video={item} />}
                </Box>
            ))}
            {/* yani  mainda rapidapi serveridan get qilingan malumotlar videos o'zgaruvchida va bu videos componentiga props orqali jo'natilib map qilinib videocart componentga jo'natildi va channelIdham main jsxda rapidapidan get qilinsa bu videosni ichida bu yerga props orqali keladimi?????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????  */}
        </Stack>
    );
};

export default Videos;
